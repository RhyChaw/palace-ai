# Examples

Pre-built palaces under **`examples/worked/`** (e.g. a **requests** snapshot for demos).  
Extraction caches under `*/palace-out/cache/` are gitignored; regenerate locally if you change the builder.

Use `--path examples/worked/requests` with `palace query` / `palace serve` as in the root README.
